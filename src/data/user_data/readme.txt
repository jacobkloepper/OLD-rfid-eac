In this folder belongs a file "users.db" in which an sql database can be setup. This folder can be created in the Python script data_handler.py by uncommenting the block in the initializer.

This directory should be on a remote server to which the program can upload.